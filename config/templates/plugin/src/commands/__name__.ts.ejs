import { run } from '@onerepo/core';
import type { Builder, Handler } from '@onerepo/core';

export const command = '<%- name %>';

export const description = 'TODO: enter a description';

type Args = {
  // TODO
};

export const builder: Builder<Args> = (yargs) => yargs;

export const handler: Handler<Args> = async function handler(argv, { logger }) {
	// TODO: do something
  logger.error('Nothing to do');
};
