---
import Default from '@astrojs/starlight/components/Head.astro';
---

<Default />
<meta
	property="og:image"
	name="twitter:image"
	content={`${Astro.url.origin}/open-graph${
		Astro.url.pathname === '/' ? '/index.png' : Astro.url.pathname.replace(/\/$/, '.png')
	}`}
/>
